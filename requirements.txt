# ============================================================
# AskMyFile — dependencies
# Versions are PINNED to the exact versions this project was
# built and tested with (July 2026, Python 3.13), so a future
# `pip install -r requirements.txt` reproduces the same setup.
# ============================================================

flask==3.1.3
werkzeug==3.1.8
python-dotenv==1.2.2
requests==2.34.2

# --- RAG pipeline ---
numpy==2.5.1
pandas==3.0.3
faiss-cpu==1.14.3
sentence-transformers==5.6.0
torch==2.13.0
transformers==5.13.1

# --- File readers ---
PyPDF2==3.0.1
pdfplumber==0.11.10
pypdfium2==5.11.0
python-docx==1.2.0
openpyxl==3.1.5
xlrd==2.0.2
lxml==6.1.1

# --- OCR (scanned PDFs + images; also needs the Tesseract program,
# --- which run.bat installs automatically via winget) ---
pytesseract==0.3.13
Pillow==12.3.0

# --- Production WSGI Server ---
gunicorn==23.0.0
